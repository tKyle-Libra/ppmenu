# HBuilderX 抖音小程序完整工作流程

## 📋 工作流程

### 方法1：自动监听修复（推荐）⭐

**步骤1：在HBuilderX中运行项目**
```
运行 → 运行到小程序模拟器 → 抖音开发者工具
```

**步骤2：在终端运行监听脚本**
```bash
cd /Users/tong/Code/AI/product-list-vite
node scripts/watch-hbuilderx-toutiao.js
```

**步骤3：在抖音开发者工具中**
- 清除缓存 → 全部清除
- 点击刷新

**优点**：
- ✅ 一次启动，持续监听
- ✅ 每次编译后自动修复
- ✅ 无需手动操作

---

### 方法2：手动修复

**每次在HBuilderX中编译后：**

**步骤1：运行修复**
```bash
cd /Users/tong/Code/AI/product-list-vite
node scripts/fix-toutiao-build-v2.js
```

**步骤2：在抖音开发者工具中**
```
清除缓存 → 全部清除 → 刷新
```

---

## 🎯 推荐的日常开发流程

### 第一次启动

1. **打开HBuilderX**
   - 导入项目：`/Users/tong/Code/AI/product-list-vite`

2. **打开终端（Terminal）**
   ```bash
   cd /Users/tong/Code/AI/product-list-vite
   node scripts/watch-hbuilderx-toutiao.js
   ```

3. **在HBuilderX中运行**
   ```
   运行 → 运行到小程序模拟器 → 抖音开发者工具
   ```

4. **等待自动修复完成**
   - 终端会显示"✅ 修复完成！"

5. **在抖音开发者工具中**
   ```
   清除缓存 → 全部清除 → 刷新
   ```

---

### 修改代码后

**情况1：自动监听正在运行**
- 直接在HBuilderX中保存文件
- HBuilderX会自动重新编译
- 监听脚本会自动检测并修复
- 在抖音开发者工具中刷新即可

**情况2：自动监听未运行**
- 在终端运行：`node scripts/watch-hbuilderx-toutiao.js`
- 或者手动修复：`node scripts/fix-toutiao-build-v2.js`

---

## ⚠️ 重要提示

### 每次HBuilderX编译后都需要修复

uni-app编译器的问题导致：
- 编译后的文件结构不正确
- 必须运行修复脚本
- 无法绕过这个步骤

### 为什么不能自动修复？

HBuilderX不支持编译后钩子（post-build hook），所以：
- 无法在HBuilderX内部自动修复
- 需要外部脚本监听文件变化
- 监听脚本提供了最佳的自动化体验

---

## 🔍 验证修复是否成功

运行监听脚本后，终端应该显示：

```
👀 HBuilderX 抖音小程序自动修复监听器
==========================================

✅ 检测到编译完成，正在运行修复...
✅ 已修复页面逻辑文件
✅ 已修复 index.js

✨ 修复完成！
💡 现在可以在抖音开发者工具中刷新
```

---

## 📱 抖音开发者工具设置

为了获得最佳体验，建议：

1. **关闭自动保存**
   ```
   设置 → 编辑器 → 自动保存 → 关闭
   ```

2. **启用热更新**
   ```
   设置 → 编辑器 → 热更新 → 开启
   ```

3. **清除缓存快捷键**
   ```
   工具栏 → 清除缓存 → 全部清除
   ```

---

## 🚨 常见问题

### Q1: 监听脚本没有自动修复？

**A**: 检查以下几点：
- [ ] 终端是否显示"监听中"
- [ ] HBuilderX是否正在运行
- [ ] 编译是否真的完成了
- [ ] 查看终端是否有错误信息

### Q2: 修复后还是报错？

**A**: 
1. 在抖音开发者工具中：清除缓存 → 全部清除
2. 关闭抖音开发者工具
3. 重新打开项目

### Q3: 监听脚本占用终端怎么办？

**A**: 
```bash
# 在后台运行
nohup node scripts/watch-hbuilderx-toutiao.js > /tmp/hbuilderx-watch.log 2>&1 &

# 查看日志
tail -f /tmp/hbuilderx-watch.log
```

---

## 💡 最佳实践

### 开发时的工作区布局

```
┌─────────────────┐  ┌──────────────────┐
│   HBuilderX     │  │   Terminal       │
│   (编辑代码)     │  │   (监听脚本)     │
│                 │  │                  │
│  保存 → 编译     │  │  自动检测 → 修复  │
└─────────────────┘  └──────────────────┘
         │                      │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  抖音开发者工具        │
         │  (预览和调试)          │
         └──────────────────────┘
```

### 日常工作流

1. **早上开工**
   - 启动监听脚本
   - 在HBuilderX中运行项目
   - 在抖音开发者工具中清除缓存并刷新

2. **编码过程中**
   - 在HBuilderX中编辑代码
   - 保存文件（Cmd+S）
   - 等待自动修复完成（约2秒）
   - 在抖音开发者工具中刷新

3. **遇到问题时**
   - 查看监听脚本日志
   - 查看抖音开发者工具Console
   - 必要时手动运行修复

---

## 📞 需要帮助？

如果遇到问题：
1. 查看终端的监听脚本输出
2. 运行 `node scripts/test-page-registration.js` 诊断
3. 查看抖音开发者工具的Console日志

---

**最后更新**: 2026-04-10
