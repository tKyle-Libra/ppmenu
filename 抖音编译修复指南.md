# 抖音小程序编译错误修复指南

## 问题描述

在使用 uni-app 编译抖音小程序时，会出现以下错误：

```
can't find page: pages/index/index corresponding pages/index/index.js
at app.json
```

**原因**：uni-app 编译器不会在 `pages/index/` 目录下生成 `index.js` 和 `index.ttss` 文件，但抖音开发者工具期望这些文件存在。

---

## ✅ 解决方案

### 🔍 根本原因

uni-app 编译抖音小程序时存在两个问题：

1. **页面文件缺失**：不会在 `pages/index/` 目录下生成 `index.js` 和 `index.ttss` 文件
2. **异步导入问题**：`app.js` 中使用动态导入（异步），导致页面在异步任务中注册，触发错误：
   ```
   Page[pages/index/index] not found
   Invoking Page() in async task
   ```

### 方案1：自动修复（推荐）

#### 一次性修复
```bash
npm run fix:toutiao
```

#### 监听模式（推荐）
```bash
npm run fix:toutiao:watch
```

**监听模式**会在检测到文件变化时自动修复，无需手动干预。

---

### 方案2：手动修复

如果自动脚本不工作，可以手动修复以下两个问题：

#### 1. 修复 `app.js` 的异步导入问题

在 `dist/dev/mp-toutiao/app.js` 中，找到：

```javascript
const common_vendor = require("./common/vendor.js");
if (!Math) {
  "./cGFnZXMvaW5kZXgvaW5kZXgudnVl.js".then((n) => n.cGFnZXMvaW5kZXgvaW5kZXgudnVl);
}
```

修改为：

```javascript
const common_vendor = require("./common/vendor.js");
// 同步导入页面模块，避免在异步任务中注册页面
require("./cGFnZXMvaW5kZXgvaW5kZXgudnVl.js");
if (!Math) {
  "./cGFnZXMvaW5kZXgvaW5kZXgudnVl.js".then((n) => n.cGFnZXMvaW5kZXgudnVl);
}
```

#### 3. 创建 `pages/index/index.js`
在 `dist/dev/mp-toutiao/pages/index/` 目录下创建 `index.js`：

```javascript
// 页面逻辑已通过 uni-app 编译到 cGFnZXSvaW5kZXgvaW5kZXgudnVl.js 中
// 该文件会自动调用 tt.createPage() 注册页面
require("../../cGFnZXMvaW5kZXgvaW5kZXgudnVl.js");
```

#### 2. 创建 `pages/index/index.ttss`
在同一目录下创建 `index.ttss`：

```css
.page {
  min-height: 100vh;
  background-color: #f3f4f6;
}

.banner-placeholder {
  width: 100%;
  transition: height 0.3s ease-in-out;
}
```

---

## 🚀 推荐工作流程

### 开发时的工作流程

#### 终端1：运行编译
```bash
cd /Users/tong/Code/AI/product-list-vite
npm run dev:mp-toutiao
```

#### 终端2：运行修复脚本（监听模式）
```bash
cd /Users/tong/Code/AI/product-list-vite
npm run fix:toutiao:watch
```

这样，每次你修改代码触发重新编译时，修复脚本会自动补充缺失的文件。

---

## 📋 使用说明

### 修复脚本功能

| 命令 | 功能 | 说明 |
|------|------|------|
| `npm run fix:toutiao` | 一次性修复 | 立即创建缺失的文件 |
| `npm run fix:toutiao:watch` | 监听模式 | 自动检测并修复 |
| `node scripts/fix-toutiao-build.js` | 查看帮助 | 显示所有可用命令 |

### 监听模式工作原理

1. 监听 `dist/dev/mp-toutiao/pages/index/` 目录变化
2. 监听 `cGFnZXMvaW5kZXgvaW5kZXgudnVl.js` 文件变化
3. 检测到变化后，延迟1秒自动修复
4. 按 `Ctrl+C` 停止监听

---

## ⚠️ 注意事项

1. **每次重新编译后**，dist 目录会被清空，需要重新运行修复脚本
2. **推荐使用监听模式**，可以自动处理重新编译的情况
3. **文件位置**：修复的文件在 `dist/dev/mp-toutiao/pages/index/` 目录下
4. **不影响源代码**：此脚本只操作编译后的文件，不影响源代码

---

## 🔍 验证修复

运行修复脚本后，检查以下文件是否存在：

```bash
ls -la dist/dev/mp-toutiao/pages/index/
```

应该看到：
- ✅ `index.js` - 页面逻辑文件
- ✅ `index.json` - 页面配置文件（编译器生成）
- ✅ `index.ttml` - 页面模板文件（编译器生成）
- ✅ `index.ttss` - 页面样式文件

如果所有文件都存在，说明修复成功！现在可以在抖音开发者工具中打开项目了。

---

## 🎯 下一步

1. 运行修复脚本：`npm run fix:toutiao`
2. 打开抖音开发者工具
3. 导入项目：`dist/dev/mp-toutiao` 目录
4. 开始调试和开发

---

## 📞 遇到问题？

如果脚本无法正常工作：

1. 检查 Node.js 版本（建议 v14+）
2. 确保 `dist/dev/mp-toutiao` 目录存在
3. 查看脚本输出的错误信息
4. 尝试手动创建文件（见方案2）

---

**最后更新**: 2026-04-10
