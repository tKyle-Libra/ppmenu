# PP家的菜单 - 生产/测试数据分离使用说明

## 📋 目录

1. [环境说明](#环境说明)
2. [推送测试数据到GitHub](#推送测试数据到github)
3. [切换环境](#切换环境)
4. [日常使用流程](#日常使用流程)
5. [数据更新](#数据更新)
6. [常见问题](#常见问题)

---

## 环境说明

### 📦 ppmenu_assets 仓库结构

```
ppmenu_assets (GitHub仓库)
├── main分支 (生产数据)                    # 正式上线使用
│   ├── db/
│   │   ├── data-page1.json 到 data-page26.json
│   │   └── 共509条商品（每页20条）
│   └── filter-dimensions.json
│
└── test分支 (测试数据)                    # 开发调试使用
    ├── db/
    │   ├── data-page1.json
    │   ├── data-page2.json
    │   └── data-page3.json
    │   └── 共30条商品（每页10条）
    └── filter-dimensions.json
```

### 🎯 小程序项目配置

项目位置：`/Users/tong/Code/AI/product-list-vite/`

- **测试环境**: 使用 `ppmenu_assets@test` (30条商品)
- **生产环境**: 使用 `ppmenu_assets@main` (509条商品)

---

## 推送测试数据到GitHub

### 📍 测试数据位置

```
/Users/tong/Code/AI/ppmenu-assets-test/
```

### 🚀 推送步骤

#### 方法1：使用GitHub CLI（最简单）⭐

```bash
# 1. 安装GitHub CLI
brew install gh

# 2. 登录GitHub
gh auth login

# 3. 进入测试数据目录
cd /Users/tong/Code/AI/ppmenu-assets-test

# 4. 推送到test分支
git push -u origin test
```

#### 方法2：使用SSH密钥（推荐）

```bash
# 1. 进入测试数据目录
cd /Users/tong/Code/AI/ppmenu-assets-test

# 2. 修改远程地址为SSH（如果需要）
git remote set-url origin git@github.com:tKyle-Libra/ppmenu_assets.git

# 3. 推送
git push -u origin test
```

#### 方法3：使用Personal Access Token

```bash
cd /Users/tong/Code/AI/ppmenu-assets-test
git push -u origin test
# 输入GitHub用户名和Token（不是密码）
```

### ✅ 推送成功后

**查看test分支**: https://github.com/tKyle-Libra/ppmenu_assets/tree/test

**测试CDN地址**:
```
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page1.json
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page2.json
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page3.json
```

---

## 切换环境

### 🔄 切换到测试环境（开发调试）

```bash
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js development
```

**输出**:
```
🔄 切换ppmenu_assets环境: development

✅ 环境已切换！

📊 测试环境配置:
   数据源: ppmenu_assets@test 分支
   CDN: https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/
   数据量: 30条商品 (3页，每页10条)
   用途: 开发调试

💡 下一步：
   1. 重新编译项目
   2. 在浏览器/小程序中查看效果
```

### 🔄 切换到生产环境（正式上线）

```bash
node scripts/set-env.js production
```

**输出**:
```
🔄 切换ppmenu_assets环境: production

✅ 环境已切换！

📊 生产环境配置:
   数据源: ppmenu_assets@main 分支
   CDN: https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@main/
   数据量: 509条商品 (26页，每页20条)
   用途: 正式上线

💡 下一步：
   1. 重新编译项目
   2. 在浏览器/小程序中查看效果
```

### 🔍 查看当前环境

```bash
node -e "require('./src/utils/config.js').showCurrentEnv()"
```

**输出**:
```
====================
📱 当前环境信息
====================
环境: development
名称: 测试环境
数据源: ppmenu_assets@test
商品数量: 30条 (3页)
每页数量: 10条
CDN地址:
  https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/
====================
```

---

## 日常使用流程

### 📝 开发阶段（推荐使用测试环境）

```bash
# 1. 切换到测试环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js development

# 2. 运行H5开发服务器
npm run dev:h5

# 3. 浏览器访问
open http://localhost:5173
```

**优势**:
- ✅ 只有30条商品，加载快
- ✅ 方便测试各种筛选功能
- ✅ 不消耗过多CDN流量
- ✅ 快速迭代开发

### 🧪 测试阶段（使用生产环境）

```bash
# 1. 切换到生产环境
node scripts/set-env.js production

# 2. 重新编译
# 停止当前编译（Ctrl+C）
npm run dev:h5

# 3. 浏览器访问
open http://localhost:5173
```

**目的**:
- 验证完整数据（509条）
- 测试性能
- 检查边界情况
- 准备正式上线

### 🚀 正式上线

```bash
# 1. 确保在生产环境
node -e "require('./src/utils/config.js').showCurrentEnv()"

# 2. 编译生产版本
npm run build:h5
npm run build:mp-weixin
npm run build:mp-toutiao
```

---

## 数据更新

### 更新测试数据

```bash
# 1. 重新生成测试数据（从生产数据采样）
cd /Users/tong/Code/AI/product-list-vite
node scripts/generate-ppmenu-test-data.js

# 2. 进入测试数据目录
cd /Users/tong/Code/AI/ppmenu-assets-test

# 3. 提交更新
git add .
git commit -m "更新测试数据"
git push origin test
```

### 从GitHub同步最新测试数据

```bash
cd /Users/tong/Code/AI/product-list-vite
node scripts/sync-ppmenu-test-data.js
```

这个脚本会：
1. 克隆ppmenu_assets的main分支
2. 提取前3页作为测试数据
3. 自动更新筛选配置数量
4. 保存到 `ppmenu-assets-test/` 目录

---

## 快速命令参考

### 环境切换
```bash
node scripts/set-env.js development   # 测试环境（30条）
node scripts/set-env.js production    # 生产环境（509条）
```

### 查看环境
```bash
node -e "require('./src/utils/config.js').showCurrentEnv()"
```

### 数据管理
```bash
node scripts/generate-ppmenu-test-data.js    # 生成测试数据
node scripts/sync-ppmenu-test-data.js       # 从GitHub同步
```

### 运行项目
```bash
npm run dev:h5              # H5模式
npm run dev:mp-weixin        # 微信小程序
npm run dev:mp-toutiao       # 抖音小程序
```

---

## 环境对比

| 特性 | 测试环境 (@test) | 生产环境 (@main) |
|------|-------------------|-------------------|
| **GitHub分支** | test | main |
| **商品数量** | 30条 | 509条 |
| **页数** | 3页 | 26页 |
| **每页数量** | 10条 | 20条 |
| **CDN地址** | `...@test/` | `...@main/` |
| **数据量大小** | ~10KB | ~150KB |
| **加载速度** | 很快 | 较快 |
| **适用场景** | 开发调试 | 正式上线 |

---

## ⚠️ 注意事项

### 1. 切换环境后必须重新编译

修改环境配置后，项目需要重新编译才能生效：
```bash
# 停止当前编译（按 Ctrl+C）
# 重新运行
npm run dev:h5
```

### 2. CDN缓存

CDN (jsdelivr) 会有缓存，更新数据后：
- 测试环境：等待5-10分钟CDN更新
- 或者添加版本号：`@test-v1.0.0`
- 或清除浏览器缓存

### 3. 不要合并分支

⚠️ **重要**: 不要将test分支合并到main分支
- test分支只用于开发调试
- main分支用于生产数据
- 两者数据量不同，不能混合

### 4. 筛选配置

test分支有独立的筛选配置：
- 商品数量已更新为30条
- 各筛选选项的数量也相应调整
- 确保筛选结果的准确性

---

## 💡 最佳实践

### 推荐工作流

```
1. 开发新功能
   └─> 使用测试环境（30条）
       └─> 快速迭代，方便调试

2. 功能测试
   └─> 切换到生产环境（509条）
       └─> 验证完整数据，测试性能

3. 正式上线
   └─> 使用生产环境
       └─> 完整功能，最优性能
```

### 开发时的工作区布局

```
┌─────────────────┐  ┌──────────────────┐
│   编辑器         │  │   终端            │
│   (写代码)       │  │   (运行项目)      │
│                 │  │                  │
│  保存文件        │  │  npm run dev:h5   │
│                 │  │                  │
└─────────────────┘  └──────────────────┘
         │                      │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  浏览器              │
         │  (localhost:5173)   │
         │  - 测试环境: 30条     │
         │  - 生产环境: 509条   │
         └──────────────────────┘
```

---

## 📞 获取帮助

### 文档位置

- **推送说明**: `/Users/tong/Code/AI/ppmenu-assets-test/推送说明.md`
- **实施指南**: `/Users/tong/Code/AI/product-list-vite/ppmenu_assets数据分离实施指南.md`
- **数据切换指南**: `/Users/tong/Code/AI/product-list-vite/数据环境切换指南.md`

### 常用命令

```bash
# 快速切换环境（从任意目录）
cd /Users/tong/Code/AI/product-list-vite && node scripts/set-env.js development

# 查看环境信息（从任意目录）
node -e "require('/Users/tong/Code/AI/product-list-vite/src/utils/config.js').showCurrentEnv()"

# 进入项目目录
cd /Users/tong/Code/AI/product-list-vite

# 进入测试数据目录
cd /Users/tong/Code/AI/ppmenu-assets-test
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-11  
**维护者**: PP家的菜单开发团队
