# 筛选问题修复指南

## 问题1：只有第一次出现loading

### 原因
- 骨架屏的显示时间太短（100ms）
- 需要增加显示时间

### 解决方案
在 `handleFilterChange` 方法中找到：
```javascript
this.$nextTick(() => {
  setTimeout(() => {
    this.applyFilter()
  }, 100)
})
```

改为：
```javascript
this.$nextTick(() => {
  setTimeout(() => {
    this.applyFilter()
  }, 300)  // 增加到 300ms
})
```

---

## 问题2：没有弹出"暂无商品"

### 原因
- 筛选无数据时，立即重置，products 从不为空
- 空状态条件永远不满足

### 解决方案
在 `applyFilter` 方法中找到 `if (needReset)` 部分：

**查找这段代码（大约第220-243行）：**
```javascript
if (needReset) {
  console.log('🔄 需要重置筛选条件')
  console.log('📺 显示骨架屏（设置 isFiltering = true）')

  this.isFiltering = true

  this.$nextTick(() => {
    setTimeout(() => {
      console.log('⏰ 延迟结束，重新执行筛选')
      this.isFiltering = false
      this.applyFilter()
    }, 300)
  })
  return
}
```

**替换为：**
```javascript
if (needReset) {
  console.log('🔄 需要重置筛选条件')
  console.log('📦 暂时清空显示，显示空状态')
  
  // 关键：暂时清空显示数据
  this.displayProducts = []
  
  // 延迟 800ms 后重置
  setTimeout(() => {
    console.log('⏰ 延迟结束，重新执行筛选')
    this.applyFilter()
  }, 800)
  return
}
```

---

## 修改后的效果

### 点击"新品"时：
1. 📺 显示骨架屏（300ms）
2. 📦 清空数据 → 看到"暂无商品"提示
3. ⏰ 等待 800ms
4. ✅ 自动重置为"全部"
5. 📋 显示所有商品

### 完整时间线：
```
0ms:   点击"新品" → 骨架屏显示
300ms: 骨架屏隐藏，数据清空
      → 用户看到"暂无商品"
800ms: 自动重置筛选
      → 显示所有商品
```

---

## 修改完成后

1. 保存文件
2. 刷新浏览器
3. 点击"新品"测试
4. 应该能看到：
   - ✅ 骨架屏（闪烁的卡片）
   - ✅ "暂无商品"提示
   - ✅ 自动回到"全部"
   - ✅ 显示所有商品
