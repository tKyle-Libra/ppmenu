# 快速开始指南 - PP家的菜单

## 🎯 环境说明

两个 GitHub 仓库：
- **ppmenu_assets_test** = 待部署测试数据（509条）
- **ppmenu_assets** = 正式生产数据（509条）

两个项目目录：
- `/Users/tong/Code/AI/ppmenu_assets_test/` = 测试数据仓库
- `/Users/tong/Code/AI/ppmenu_assets/` = 生产数据仓库

---

## 🚀 日常使用

### 1. 测试新数据（使用 ppmenu_assets_test）

```bash
# 切换到待部署测试环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js development

# 运行项目
npm run dev:h5

# 浏览器访问: http://localhost:5173
```

**当前配置**：
- 📦 仓库：ppmenu_assets_test
- 📊 数据：509条（26页）
- 🎯 用途：测试验证

### 2. 数据测试通过，提交到 ppmenu_assets

```bash
# 1. 在测试仓库中提交数据
cd /Users/tong/Code/AI/ppmenu_assets_test
git add .
git commit -m "更新商品数据"
git push origin main

# 2. 同步数据到生产仓库
cd /Users/tong/Code/AI/ppmenu_assets
# 复制 db 目录内容
cp -r ../ppmenu_assets_test/db/* ./db/
git add .
git commit -m "同步数据"
git push origin main

# 3. 切换项目到生产环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js production

# 4. 重新运行项目
npm run dev:h5
```

---

## 📊 环境对比

| 环境 | 仓库 | 数据量 | 命令 |
|------|------|--------|------|
| 待部署测试 | ppmenu_assets_test | 509条 | `node scripts/set-env.js development` |
| 正式生产 | ppmenu_assets | 509条 | `node scripts/set-env.js production` |

---

## 💡 快速命令

### 切换环境
```bash
node scripts/set-env.js development   # 待部署测试（ppmenu_assets_test）
node scripts/set-env.js production    # 正式生产（ppmenu_assets）
```

### 查看当前环境
```bash
node -e "require('./src/utils/config.js').showCurrentEnv()"
```

### 运行项目
```bash
npm run dev:h5            # H5模式
npm run dev:mp-weixin     # 微信小程序
npm run dev:mp-toutiao    # 抖音小程序
```

---

## ⚠️ 注意事项

1. **切换环境后必须重新编译**
   ```bash
   # 停止当前编译（Ctrl+C）
   # 切换环境
   node scripts/set-env.js [environment]
   # 重新运行
   npm run dev:h5
   ```

2. **两个仓库的 db 目录保持一致**
   - ppmenu_assets_test/db/ = 测试数据
   - ppmenu_assets/db/ = 生产数据
   - 数据量都是 509 条

3. **数据更新流程**
   ```
   更新测试数据 → 验证 → 提交到测试仓库
   → 同步到生产仓库 → 切换环境 → 面向用户
   ```

---

**详细文档**: 查看 `环境说明和使用指南.md`
