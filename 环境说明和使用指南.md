# PP家的菜单 - 环境说明和使用指南

## 🎯 环境定义

### 两种环境

| 环境 | 代码 | 仓库 | 商品数 | 用途 |
|------|------|------|--------|------|
| **待部署测试** | `development` | ppmenu_assets_test | 509条 | 数据测试和验证 |
| **正式生产** | `production` | ppmenu_assets | 509条 | 面向线上用户 |

---

## 🚀 使用流程

### 工作流程

```
┌─────────────────────────────────────────────────────┐
│  1. 在 ppmenu_assets_test 中测试数据                 │
│     └─> 使用 development 环境                        │
│         └─> npm run dev:h5                          │
│             └─> 测试509条完整数据                    │
└─────────────────────────────────────────────────────┘
                        ↓
              测试没有问题 ✅
                        ↓
┌─────────────────────────────────────────────────────┐
│  2. 提交数据到 ppmenu_assets                         │
│     └─> 在 ppmenu_assets_test 仓库中                │
│         └─> git add .                               │
│             └─> git commit -m "更新数据"            │
│                 └─> git push origin main           │
│     └─> 复制数据到 ppmenu_assets 仓库               │
│         └─> 或使用 GitHub 同步脚本                  │
└─────────────────────────────────────────────────────┘
                        ↓
              数据已同步到生产仓库 ✅
                        ↓
┌─────────────────────────────────────────────────────┐
│  3. 切换到 production 环境面向线上用户               │
│     └─> node scripts/set-env.js production          │
│         └─> npm run dev:h5                          │
│             └─> 从 ppmenu_assets 获取数据           │
└─────────────────────────────────────────────────────┘
```

---

## 📋 环境对比详解

### development vs production

**相同点**：
- 都有509条完整商品数据
- 数据内容完全相同
- 页数和每页数量相同

**不同点**：
- **development（待部署测试）**：
  - 使用 `ppmenu_assets_test` 仓库
  - CDN: `...@tKyle-Libra/ppmenu_assets_test@main/`
  - 用于测试和验证数据
  - 测试通过后提交到 ppmenu_assets

- **production（正式生产）**：
  - 使用 `ppmenu_assets` 仓库
  - CDN: `...@tKyle-Libra/ppmenu_assets@main/`
  - 面向真实线上用户
  - 正式发布使用

---

## 🔄 快速命令

### 环境切换

```bash
# 待部署测试环境（ppmenu_assets_test）
node scripts/set-env.js development

# 正式生产环境（ppmenu_assets）
node scripts/set-env.js production
```

### 查看当前环境

```bash
node -e "require('./src/utils/config.js').showCurrentEnv()"
```

### 运行项目

```bash
npm run dev:h5               # H5模式
npm run dev:mp-weixin        # 微信小程序
npm run dev:mp-toutiao       # 抖音小程序
```

---

## 📊 环境配置对照表

| 配置项 | development | production |
|--------|-------------|------------|
| **GitHub仓库** | ppmenu_assets_test | ppmenu_assets |
| **GitHub分支** | @main | @main |
| **商品数量** | 509 | 509 |
| **页数** | 26 | 26 |
| **每页数量** | 20 | 20 |
| **CDN地址** | ...@ppmenu_assets_test@main/ | ...@ppmenu_assets@main/ |
| **用途** | 待部署测试 | 正式上线 |
| **面向用户** | 开发者/QA | 真实用户 |

---

## 🎯 实际使用示例

### 场景1：测试新数据

```bash
# 1. 使用待部署测试环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js development

# 2. 运行项目测试数据
npm run dev:h5

# 3. 在浏览器中访问 http://localhost:5173
# 4. 测试509条完整数据
```

### 场景2：数据测试通过，准备上线

```bash
# 1. 在 ppmenu_assets_test 仓库中提交数据
cd /Users/tong/Code/AI/ppmenu_assets_test
git add .
git commit -m "更新商品数据"
git push origin main

# 2. 同步数据到 ppmenu_assets 仓库
# （手动复制或使用同步脚本）

# 3. 切换到正式生产环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js production

# 4. 运行项目验证
npm run dev:h5

# 5. 确认从 ppmenu_assets 获取数据
```

### 场景3：正式发布

```bash
# 确保在 production 环境
node -e "require('./src/utils/config.js').showCurrentEnv()"

# 编译生产版本
npm run build:h5
npm run build:mp-weixin
npm run build:mp-toutiao

# 部署到服务器
```

---

## ⚠️ 重要提示

### 1. 环境切换注意事项

⚠️ **每次切换环境后必须重新编译项目**

```bash
# 停止当前编译（Ctrl+C）
# 切换环境
node scripts/set-env.js [environment]
# 重新编译
npm run dev:h5
```

### 2. 两个仓库的 db 目录保持一致

- `ppmenu_assets_test/db/` = 待部署测试数据（509条）
- `ppmenu_assets/db/` = 正式生产数据（509条）

**数据更新流程**：
1. 在 `ppmenu_assets_test` 中更新数据
2. 测试验证没有问题
3. 提交到 `ppmenu_assets_test` 仓库
4. 复制/同步到 `ppmenu_assets` 仓库
5. 提交到 `ppmenu_assets` 仓库
6. 切换项目到 production 环境

### 3. CDN 缓存

CDN (jsdelivr) 会有缓存，更新数据后：
- 等待5-10分钟 CDN 更新
- 或清除浏览器缓存
- 或添加版本号：`@main-v1.0.0`

---

## 💡 推荐工作流程

### 日常工作流

```
1. 开发/测试新数据
   └─> development 环境（ppmenu_assets_test）
       └─> 本地测试验证
       └─> 确认数据正确

2. 提交测试数据
   └─> git push 到 ppmenu_assets_test
       └─> 仓库已更新

3. 同步到生产仓库
   └─> 复制数据到 ppmenu_assets
       └─> git push 到 ppmenu_assets
       └─> 生产仓库已更新

4. 切换到生产环境
   └─> node scripts/set-env.js production
       └─> npm run dev:h5
       └─> 面向线上用户
```

---

## 🔧 脚本说明

### set-env.js

用于快速切换环境：
- 自动修改 `src/utils/config.js`
- 显示当前环境配置
- 提供下一步操作提示

### showCurrentEnv()

显示详细的环境信息：
- 环境类型和名称
- 数据源和 CDN 地址
- 商品数量和配置
- 用途说明

---

## 📝 配置示例

### 待部署测试环境配置
```javascript
// src/utils/config.js
export const CURRENT_ENV = ENVIRONMENT.DEVELOPMENT
```

**效果**：
- 仓库：ppmenu_assets_test
- CDN：https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/
- 数据：509条完整数据

### 正式生产环境配置
```javascript
// src/utils/config.js
export const CURRENT_ENV = ENVIRONMENT.PRODUCTION
```

**效果**：
- 仓库：ppmenu_assets
- CDN：https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@main/
- 数据：509条完整数据

---

## 📚 相关文档

- **快速开始**: `快速开始.md`
- **完整指南**: `使用说明-生产测试数据分离.md`
- **仓库推送**: `/Users/tong/Code/AI/ppmenu-assets-test/推送说明.md`

---

**文档版本**: v3.0
**最后更新**: 2026-04-11
**环境支持**: development / production
