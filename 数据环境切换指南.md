# 生产数据和测试数据分离指南

## 📋 概述

项目现在支持**生产环境**和**测试环境**的数据分离，便于开发和调试。

---

## 🎯 环境说明

### 生产环境 (PRODUCTION)
- **数据源**: GitHub CDN
- **数据量**: 509条商品（26页，每页20条）
- **用途**: 正式上线使用
- **特点**: 完整数据，从CDN加载

### 测试环境 (DEVELOPMENT)
- **数据源**: 本地测试数据
- **数据量**: 30条商品（3页，每页10条）
- **用途**: 开发调试使用
- **特点**: 数据量少，加载快，便于调试

---

## 🔄 切换环境

### 方法1：修改配置文件（推荐）

编辑 `src/utils/config.js` 文件第10行：

```javascript
// 切换到测试环境
export const CURRENT_ENV = ENVIRONMENT.DEVELOPMENT

// 切换到生产环境
export const CURRENT_ENV = ENVIRONMENT.PRODUCTION
```

然后重新编译项目。

### 方法2：使用快捷脚本

```bash
# 切换到测试环境
node scripts/set-env.js development

# 切换到生产环境  
node scripts/set-env.js production
```

---

## 📁 目录结构

```
product-list-vite/
├── data-pages/db/           # 生产数据（509条）
│   ├── data-page1.json
│   ├── data-page2.json
│   └── ... (26个文件)
│
├── public/data-test/        # 测试数据（30条）
│   ├── data-page1.json
│   ├── data-page2.json
│   ├── data-page3.json
│   ├── filter-dimensions.json
│   ├── filter-categories.json
│   └── README.md
│
└── src/
    ├── utils/
    │   └── config.js        # 环境配置
    └── data/
        ├── filter-dimensions.json
        └── filter-categories.json
```

---

## 🔧 生成测试数据

### 重新生成测试数据

```bash
node scripts/generate-test-data.js
```

**脚本功能**：
- 从生产数据中随机采样30条商品
- 分成3页，每页10条
- 更新筛选配置的数量
- 生成README文档

### 自定义测试数据

编辑 `scripts/generate-test-data.js` 文件的配置：

```javascript
const TEST_CONFIG = {
  pages: 5,           // 测试数据总页数
  pageSize: 5,        // 每页数据量
  sampleFrom: [1, 2, 3] // 从这些页中采样
};
```

---

## 💡 使用建议

### 开发阶段
```
使用测试环境（DEVELOPMENT）
✅ 数据量少，加载快
✅ 便于查看各种情况
✅ 不消耗CDN流量
✅ 筛选测试更方便
```

### 测试阶段
```
使用生产环境（PRODUCTION）
✅ 真实数据量，测试性能
✅ 验证所有商品是否正常
✅ 测试分页加载
✅ 检查筛选准确性
```

### 生产上线
```
使用生产环境（PRODUCTION）
✅ 完整数据
✅ CDN加速
✅ 最优性能
```

---

## 🎛️ 配置说明

### 开发环境配置

```javascript
DEVELOPMENT_CONFIG = {
  BASE_URL: 'http://localhost:5173/',  // 本地服务器
  DATA_BASE_URL: '/data-test/',         // 测试数据目录
  DATA_SOURCE: 'local',                 // 使用本地数据
  TOTAL_PAGES: 3,                       // 3页数据
  PAGE_SIZE: 10                         // 每页10条
}
```

### 生产环境配置

```javascript
PRODUCTION_CONFIG = {
  BASE_URL: 'https://cdn.jsdelivr.net/gh/...',  // CDN地址
  DATA_BASE_URL: '/db/',                        // 生产数据目录
  DATA_SOURCE: 'cdn',                            // 使用CDN数据
  TOTAL_PAGES: 26,                               // 26页数据
  PAGE_SIZE: 20                                   // 每页20条
}
```

---

## 🔍 查看当前环境

### 方法1：查看配置文件

```bash
cat src/utils/config.js | grep CURRENT_ENV
```

### 方法2：在Console中查看

打开浏览器/小程序的Console，输入：
```javascript
console.log(require('@/utils/config.js').getEnvInfo())
```

### 方法3：查看页面日志

- 开发环境会显示：`📥 加载测试数据: 第X页`
- 生产环境不会显示此日志

---

## ⚠️ 注意事项

### 1. 切换环境后必须重新编译

修改 `config.js` 后，项目需要重新编译才能生效：
```bash
# 停止当前编译 (Ctrl+C)
# 重新运行
npm run dev:h5
# 或
npm run dev:mp-toutiao
```

### 2. 测试数据只在H5模式下有效

测试数据放在 `public/` 目录，只能通过HTTP访问：
- ✅ H5模式：可以访问
- ❌ 小程序模式：需要将测试数据复制到项目目录

### 3. 筛选配置独立

测试数据有独立的筛选配置：
- 生产：`src/data/filter-dimensions.json`
- 测试：`public/data-test/filter-dimensions.json

两个环境的商品数量不同，筛选结果数量也会不同。

---

## 🚀 快速开始

### 开发新功能

```bash
# 1. 切换到测试环境
# 编辑 src/utils/config.js
# CURRENT_ENV = ENVIRONMENT.DEVELOPMENT

# 2. 启动H5开发服务器
npm run dev:h5

# 3. 浏览器访问
http://localhost:5173
```

### 验证生产环境

```bash
# 1. 切换到生产环境
# 编辑 src/utils/config.js
# CURRENT_ENV = ENVIRONMENT.PRODUCTION

# 2. 重新编译
npm run dev:h5

# 3. 浏览器访问
http://localhost:5173
```

---

## 📞 常见问题

### Q1: 切换环境后数据没变化？

**A**: 确保重新编译了项目，并且清除了浏览器缓存。

### Q2: 测试数据加载失败？

**A**: 
- 确保H5开发服务器正在运行
- 检查 `public/data-test/` 目录是否存在
- 查看浏览器Console的错误信息

### Q3: 想要更多测试数据？

**A**: 
```bash
# 重新生成测试数据（可以修改脚本配置）
node scripts/generate-test-data.js
```

---

**最后更新**: 2026-04-11
