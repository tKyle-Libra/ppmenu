# 抖音小程序网络请求错误诊断和修复

## ❌ 错误信息
```
加载数据失败: {errMsg: "request:fail protocol mismatch", errNo: 20000, errorCode: 0, errorType: "F"}
```

## 🔍 问题分析

### 已排除的原因
1. ✅ URL 格式正确
   - 完整URL: `https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/db/data-page1.json`
   - 协议: https://
   - 域名: cdn.jsdelivr.net

2. ✅ CDN 可访问
   - 测试结果: HTTP/2 200

3. ✅ 已开启"不校验合法域名"

### 可能的原因

**抖音小程序对 HTTPS 证书的验证**
- 抖音小程序可能对 CDN 的 HTTPS 证书有特殊要求
- jsdelivr 的证书可能不被抖音小程序信任

---

## ✅ 解决方案

### 方案1：使用 HTTP 代理（测试用，快速验证）

修改配置文件，使用 HTTP 而不是 HTTPS：

```javascript
// 临时修改 src/utils/config.js
export const DEVELOPMENT_CONFIG = {
  BASE_URL: 'http://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/',
  // ...
}
```

### 方案2：使用备用 CDN（推荐）

使用 GitHub 原始地址或其他 CDN：

```javascript
// 备选方案1: GitHub 原始地址（慢但稳定）
export const DEVELOPMENT_CONFIG = {
  BASE_URL: 'https://raw.githubusercontent.com/tKyle-Libra/ppmenu_assets_test/main/',
  // ...
}

// 备选方案2: 使用 gh-proxy（国内访问快）
export const DEVELOPMENT_CONFIG = {
  BASE_URL: 'https://gh-proxy.com/https://github.com/tKyle-Libra/ppmenu_assets_test/raw/main/',
  // ...
}
```

### 方案3：配置服务器域名（生产环境）

在抖音开放平台配置：
1. 登录 https://developer.open-douyin.com/
2. 进入 小程序 → 开发 → 开发设置
3. 添加服务器域名：`https://cdn.jsdelivr.net`

---

## 🚀 立即测试方案

### 步骤1：使用 GitHub 原始地址

修改 `/Users/tong/Code/AI/product-list-vite/src/utils/config.js`:

```javascript
export const DEVELOPMENT_CONFIG = {
  BASE_URL: 'https://raw.githubusercontent.com/tKyle-Libra/ppmenu_assets_test/main/',
  // 其他配置保持不变
}
```

### 步骤2：重新编译
```bash
cd /Users/tong/Code/AI/product-list-vite
npm run dev:mp-toutiao
```

### 步骤3：在抖音开发者工具中
- 点击"编译"
- 查看控制台
- 应该能看到数据加载成功

---

## 📋 完整的配置选项

### 选项1: GitHub Raw（最稳定，但慢）
```javascript
BASE_URL: 'https://raw.githubusercontent.com/tKyle-Libra/ppmenu_assets_test/main/'
```
- ✅ 优点：最稳定，抖音小程序完全支持
- ❌ 缺点：国内访问较慢

### 选项2: jsDelivr CDN（当前方案）
```javascript
BASE_URL: 'https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/'
```
- ✅ 优点：速度快，有CDN加速
- ❌ 缺点：可能与抖音小程序有证书问题

### 选项3: Staticfile CDN（国内CDN）
```javascript
BASE_URL: 'https://cdn.staticfile.net/gh/tKyle-Libra/ppmenu_assets_test@main/'
```
- ✅ 优点：国内访问快
- ❌ 缺点：需要验证是否支持

### 选项4: 使用自建服务器
```javascript
BASE_URL: 'https://your-cdn.com/ppmenu_assets_test/'
```
- ✅ 优点：完全可控
- ❌ 缺点：需要额外部署

---

## 🔧 快速修复脚本

创建一个修复脚本 `fix-network.sh`:

```bash
#!/bin/bash

echo "🔧 修复抖音小程序网络请求问题"
echo ""

echo "备份原配置..."
cp src/utils/config.js src/utils/config.js.backup

echo "修改为 GitHub Raw 地址..."
sed -i '' 's|https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/|https://raw.githubusercontent.com/tKyle-Libra/ppmenu_assets_test/main/|g' src/utils/config.js

echo "✅ 修复完成！"
echo ""
echo "请重新编译项目："
echo "npm run dev:mp-toutiao"
```

---

## 📊 测试验证

### 1. 测试 URL 可访问性

```bash
# 测试 GitHub Raw
curl -I "https://raw.githubusercontent.com/tKyle-Libra/ppmenu_assets_test/main/db/data-page1.json"

# 测试 jsDelivr
curl -I "https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets_test@main/db/data-page1.json"
```

### 2. 在小程序中验证

编译后查看控制台：
- ✅ 正确：`加载成功: 第1页, 20条`
- ❌ 错误：`加载数据失败: ...`

---

## 💡 推荐方案

**短期（立即测试）**:
1. 使用 GitHub Raw 地址
2. 验证功能是否正常

**长期（生产环境）**:
1. 配置自己的 CDN 服务器
2. 或使用支持的国内 CDN 服务

**目前建议**:
- 先用 GitHub Raw 验证功能
- 确认功能正常后，再优化 CDN 配置

---

**需要我帮你立即修改配置使用 GitHub Raw 吗？**
