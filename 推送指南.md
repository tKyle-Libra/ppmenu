# 提交测试数据到GitHub - 操作指南

## ✅ 已完成的步骤

1. ✅ 初始化了Git仓库
2. ✅ 创建了test分支
3. ✅ 提交了所有文件
4. ✅ 添加了远程仓库地址

## 🔐 最后一步：推送到GitHub

需要认证才能推送。请选择以下任一方式：

---

## 方法1：使用GitHub CLI（推荐）

### 安装GitHub CLI
```bash
brew install gh
```

### 登录GitHub
```bash
gh auth login
```

### 推送
```bash
cd /Users/tong/Code/AI/product-list-vite/ppmenu-assets-test
git push -u origin test
```

---

## 方法2：使用SSH密钥

### 检查SSH密钥
```bash
ls -la ~/.ssh/id_rsa.pub
```

### 如果没有，生成SSH密钥
```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

### 添加到GitHub
1. 复制公钥：
```bash
cat ~/.ssh/id_rsa.pub
```

2. 访问：https://github.com/settings/keys
3. 点击 "New SSH key"
4. 粘贴公钥
5. 点击 "Add SSH key"

### 修改远程仓库地址为SSH
```bash
cd /Users/tong/Code/AI/product-list-vite/ppmenu-assets-test
git remote set-url origin git@github.com:tKyle-Libra/ppmenu_assets.git
```

### 推送
```bash
git push -u origin test
```

---

## 方法3：使用Personal Access Token

### 1. 创建Token
访问：https://github.com/settings/tokens
- 点击 "Generate new token (classic)"
- 勾选 `repo` 权限
- 生成并复制token

### 2. 使用Token推送
```bash
cd /Users/tong/Code/AI/product-list-vite/ppmenu-assets-test
git push -u origin test
```
- 用户名：输入GitHub用户名
- 密码：粘贴Token（不是GitHub密码）

---

## 🎯 推荐操作

如果不确定使用哪个方法，**推荐使用方法2（SSH密钥）**，配置一次，以后所有项目都可以用。

---

## ✅ 推送成功后

你会看到：
```
Branch 'test' set up to track remote branch 'test' from 'origin'.
```

然后在GitHub上查看：
```
https://github.com/tKyle-Libra/ppmenu_assets/tree/test
```

---

## 🔄 测试数据地址

推送成功后，测试数据的CDN地址为：
```
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page1.json
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page2.json
https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@test/db/data-page3.json
```

---

需要我帮你配置SSH密钥吗？或者你已经有GitHub认证方式了？告诉我如何操作！
