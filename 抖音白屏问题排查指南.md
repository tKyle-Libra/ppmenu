# 抖音小程序白屏问题排查指南

## ✅ 文件检查结果

所有必需文件都已正确生成：
- ✅ app.json 配置正确
- ✅ app.js 同步导入正确
- ✅ 页面逻辑文件包含 tt.createPage 调用
- ✅ index.js 不会覆盖页面逻辑
- ✅ index.ttml 有内容（462字节）
- ✅ 所有组件都正确注册

---

## 🔍 可能的原因和解决方案

### 原因1：缓存问题（最常见）

**症状**：文件正确，但显示旧版本或白屏

**解决方案**：

1. **清除抖音开发者工具缓存**
   ```
   工具栏 → 清除缓存 → 全部清除
   ```

2. **重新编译项目**
   ```bash
   # 停止当前编译
   # 重新运行
   npm run dev:mp-toutiao
   ```

3. **重新导入项目**
   ```
   关闭抖音开发者工具
   重新打开
   重新导入项目目录
   ```

---

### 原因2：数据加载失败

**症状**：页面结构正常，但没有数据显示

**诊断步骤**：

1. **打开抖音开发者工具的 Console**
   ```
   查看 Console 标签
   查找错误信息
   ```

2. **检查网络请求**
   ```
   Network 标签
   查找失败的请求（红色）
   ```

3. **常见数据加载错误**：

   **错误1：CDN请求失败**
   ```
   解决方案：在开发者工具中关闭域名校验
   详情 → 本地设置 → 勾选"不校验合法域名"
   ```

   **错误2：跨域问题**
   ```
   解决方案：确保数据URL支持CORS
   当前使用：https://cdn.jsdelivr.net/gh/tKyle-Libra/ppmenu_assets@main/
   ```

   **错误3：数据格式错误**
   ```
   在 Console 中查看数据响应
   确认返回的是有效的JSON数组
   ```

---

### 原因3：组件渲染失败

**症状**：部分页面显示，但组件区域空白

**诊断步骤**：

1. **检查组件路径**
   - 确保 components 目录下所有组件都存在
   - 组件路径区分大小写

2. **检查组件注册**
   - 查看 pages/index/index.json
   - 确认 usingComponents 配置正确

3. **检查组件JS文件**
   - 每个组件应该有对应的 .js 文件
   - 文件中应该有 tt.createComponent 调用

---

### 原因4：真机/模拟器兼容性

**症状**：开发者工具正常，但真机白屏

**解决方案**：

1. **更新抖音开发者工具到最新版本**

2. **检查基础库版本**
   ```
   project.config.json 中的 minPlatformVersion
   建议设置为 1.0.0 或更高
   ```

3. **在真机上调试**
   ```
   预览 → 扫码预览
   在真机上查看效果
   ```

---

## 🛠️ 调试工具

### 1. 运行诊断脚本

```bash
node scripts/test-page-registration.js
```

这会检查所有文件是否正确生成。

### 2. 查看详细日志

在页面中添加调试日志：

```javascript
// src/pages/index/index.vue
onLoad() {
  console.log('===== 页面加载开始 =====');
  console.log('当前时间:', new Date().toISOString());
  this.loadFirstPage();
},

async loadFirstPage() {
  console.log('开始加载第一页...');
  // ... 其他代码
}
```

### 3. 测试简化版本

创建一个最小化测试页面：

```vue
<template>
  <view class="page">
    <text>测试页面 - 如果你看到这行文字，说明渲染正常</text>
  </view>
</template>

<script>
export default {
  onLoad() {
    console.log('测试页面加载成功');
  }
}
</script>

<style>
.page {
  padding: 20px;
  background-color: #f0f0f0;
}
</style>
```

---

## 📋 快速修复清单

按顺序尝试以下步骤：

- [ ] 1. 清除抖音开发者工具缓存
- [ ] 2. 重新编译项目：`npm run dev:mp-toutiao`
- [ ] 3. 关闭并重新打开抖音开发者工具
- [ ] 4. 运行诊断脚本：`node scripts/test-page-registration.js`
- [ ] 5. 检查 Console 是否有错误
- [ ] 6. 检查 Network 标签，查看请求是否成功
- [ ] 7. 关闭域名校验：详情 → 本地设置 → 不校验合法域名
- [ ] 8. 尝试真机预览
- [ ] 9. 更新抖音开发者工具到最新版本
- [ ] 10. 查看 Console 日志，确认是否有"App Launch"、"App Show"日志

---

## 🎯 最可能的原因

基于你的情况（完全白屏，无错误信息），**最可能的原因是：**

### 缓存问题（80%概率）

抖音开发者工具缓存了旧的编译版本，导致页面无法正确渲染。

**立即尝试**：
1. 工具栏 → 清除缓存 → 全部清除
2. 重新编译：`npm run dev:mp-toutiao`
3. 重新打开项目

---

## 📞 仍然无法解决？

如果以上方法都尝试过仍然白屏，请提供：

1. **Console 截图**：完整的控制台日志
2. **Network 截图**：网络请求列表
3. **Source 标签截图**：确认文件是否正确加载
4. **诊断脚本输出**：`node scripts/test-page-registration.js` 的结果
5. **抖音开发者工具版本号**
6. **操作系统版本**

---

**最后更新**: 2026-04-10
