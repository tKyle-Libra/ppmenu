# PP家的菜单 - 环境说明和使用指南

## 🎯 环境定义

### 三种环境

| 环境 | 代码 | 数据源 | 商品数 | 用途 |
|------|------|--------|--------|------|
| **待生产环境** | `development` | ppmenu_assets@main | 509条 | 功能测试和验证 |
| **正式生产** | `production` | ppmenu_assets@main | 509条 | 正式上线 |
| **本地测试** | `test` | ppmenu_assets@test | 30条 | 本地调试 |

---

## 🚀 使用流程

### 阶段1：本地开发调试（使用test环境）

```bash
# 切换到本地测试环境
cd /Users/tong/Code/AI/product-list-vite
node scripts/set-env.js test

# 运行项目
npm run dev:h5
```

**特点**：
- ✅ 只有30条商品，加载很快
- ✅ 便于快速调试和开发
- ✅ 不消耗CDN流量

---

### 阶段2：功能测试验证（使用development环境）

```bash
# 切换到待生产环境
node scripts/set-env.js development

# 重新编译
npm run dev:h5
```

**特点**：
- ✅ 使用完整的509条商品数据
- ✅ 与生产环境数据一致
- ✅ 测试各种功能和性能
- ✅ 验证没有问题后准备上线

---

### 阶段3：正式上线（使用production环境）

```bash
# 切换到正式生产环境
node scripts/set-env.js production

# 重新编译
npm run dev:h5
```

**特点**：
- ✅ 正式环境配置
- ✅ 数据与development相同
- ✅ 可以根据需要添加生产环境特有的配置
- ✅ 用于正式发布

---

## 📋 环境对比详解

### development vs production

**相同点**：
- 都使用ppmenu_assets@main分支
- 都有509条完整商品数据
- 数据完全相同

**不同点**：
- **development** = 待生产测试阶段
  - 用于测试验证
  - 可能开启调试日志
  - 可以使用测试服务器
  
- **production** = 正式生产环境
  - 用于正式上线
  - 关闭调试日志
  - 使用正式服务器
  - 性能优化

### test环境（独立）

- 使用ppmenu_assets@test分支
- 只有30条商品
- 纯粹用于开发调试
- 与生产/待生产环境完全隔离

---

## 💡 推荐工作流程

```
1. 本地开发阶段
   └─> test环境（30条）
       └─> 快速迭代，调试代码

2. 功能完成，内部测试
   └─> development环境（509条）
       └─> 完整测试，验证功能
       └─> 发现问题返回步骤1

3. 测试通过，准备上线
   └─> production环境（509条）
       └─> 正式发布
       └─> 面向真实用户
```

---

## 🔄 快速命令

### 环境切换
```bash
# 本地测试环境（30条）
node scripts/set-env.js test

# 待生产环境（509条，测试验证）
node scripts/set-env.js development

# 正式生产环境（509条）
node scripts/set-env.js production
```

### 查看当前环境
```bash
node -e "require('./src/utils/config.js').showCurrentEnv()"
```

### 运行项目
```bash
npm run dev:h5               # H5模式
npm run dev:mp-weixin        # 微信小程序
npm run dev:mp-toutiao       # 抖音小程序
```

---

## 📊 环境配置对照表

| 配置项 | test | development | production |
|--------|-----|--------------|-------------|
| **GitHub分支** | @test | @main | @main |
| **商品数量** | 30 | 509 | 509 |
| **页数** | 3 | 26 | 26 |
| **每页数量** | 10 | 20 | 20 |
| **CDN地址** | ...@test/ | ...@main/ | ...@main/ |
| **用途** | 本地调试 | 测试验证 | 正式上线 |
| **测试数据** | ❌ | ✅ | ✅ |
| **生产数据** | ❌ | ✅ | ✅ |
| **面向用户** | 开发者 | QA测试 | 真实用户 |

---

## 🎯 实际使用示例

### 场景1：日常开发新功能

```bash
# 使用test环境（30条）
node scripts/set-env.js test
npm run dev:h5

# 快速开发，数据加载快，容易调试
```

### 场景2：开发完成，内部测试

```bash
# 切换到development环境
node scripts/set-env.js development

# 使用完整的509条数据测试
npm run dev:h5

# 验证功能、性能、边界情况
```

### 场景3：测试通过，准备上线

```bash
# 切换到production环境
node scripts/set-env.js production

# 编译生产版本
npm run build:h5
npm run build:mp-weixin

# 部署到服务器
```

---

## ⚠️ 重要提示

### 1. development vs production 的区别

虽然两者数据相同，但配置可能不同：

**development（待生产）**:
- 可以开启详细日志
- 可以显示调试信息
- 可以连接测试服务器
- 可以使用测试API

**production（正式）**:
- 关闭调试日志
- 优化性能
- 连接生产服务器
- 使用正式API

### 2. 环境切换注意事项

⚠️ **每次切换环境后必须重新编译项目**

```bash
# 停止当前编译（Ctrl+C）
# 切换环境
node scripts/set-env.js [environment]
# 重新编译
npm run dev:h5
```

### 3. test环境的使用

test环境（30条数据）只在以下情况使用：
- ✅ 本地快速开发调试
- ✅ 验证代码逻辑
- ✅ 测试UI界面
- ❌ 不用于功能测试（数据量太少）
- ❌ 不用于性能测试

---

## 📝 配置示例

### 开发阶段配置
```javascript
// src/utils/config.js
export const CURRENT_ENV = ENVIRONMENT.TEST  // 使用30条测试数据
```

### 内部测试阶段配置
```javascript
// src/utils/config.js
export const CURRENT_ENV = ENVIRONMENT.DEVELOPMENT  // 使用509条完整数据
```

### 正式上线配置
```javascript
// src/utils/config.js
export const CURRENT_ENV = ENVIRONMENT.PRODUCTION  // 正式环境
```

---

## 🔧 脚本说明

### set-env.js
用于快速切换环境：
- 自动修改 `src/utils/config.js`
- 显示当前环境配置
- 提供下一步操作提示

### showCurrentEnv()
显示详细的环境信息：
- 环境类型和名称
- 数据源和CDN地址
- 商品数量和配置
- 用途说明

---

## 📚 相关文档

- **快速开始**: `快速开始.md`
- **完整指南**: `使用说明-生产测试数据分离.md`
- **GitHub推送**: `/Users/tong/Code/AI/ppmenu-assets-test/推送说明.md`

---

**文档版本**: v2.0  
**最后更新**: 2026-04-11  
**环境支持**: test / development / production
