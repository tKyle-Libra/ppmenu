# 抖音小程序网络请求问题 - 完整解决方案

## ❌ 问题
```
加载数据失败: {errMsg: "request:fail protocol mismatch", errNo: 20000}
```

## ✅ 已确认正常的部分
1. ✅ URL 格式完全正确
2. ✅ CDN 可以正常访问
3. ✅ 已开启"不校验合法域名"
4. ✅ 数据文件存在于 GitHub

## 🔍 根本原因分析

**抖音小程序的特殊限制**：
- 抖音小程序对 HTTPS 证书有严格要求
- GitHub Raw 的证书可能不被抖音小程序信任
- 即使开启"不校验"，某些底层协议检查仍然生效

## 💡 解决方案

### 方案1：使用本地静态数据（推荐，立即见效）

#### 步骤1：创建本地数据文件

```bash
# 复制第一页数据到项目中
mkdir -p /Users/tong/Code/AI/product-list-vite/src/static/data
cp /Users/tong/Code/AI/ppmenu_assets_test/db/data-page1.json /Users/tong/Code/AI/product-list-vite/src/static/data/
```

#### 步骤2：修改数据加载方式

创建 `/Users/tong/Code/AI/product-list-vite/src/utils/local-data.js`:

```javascript
// 本地静态数据
const PAGE_DATA = {
  1: require('@/static/data/data-page1.json')
  // 可以添加更多页面
}

export async function loadPageData(pageNum) {
  try {
    console.log('从本地加载第' + pageNum + '页数据')
    
    if (PAGE_DATA[pageNum]) {
      return PAGE_DATA[pageNum]
    } else {
      console.warn('本地没有第' + pageNum + '页数据')
      return []
    }
  } catch (error) {
    console.error('加载失败:', error)
    return []
  }
}
```

#### 步骤3：在代码中使用本地数据

修改 `src/utils/data.js`，将网络请求替换为本地加载：

```javascript
import { loadPageData } from './local-data.js'

// 使用本地数据而不是网络请求
```

### 方案2：使用抖音云函数（生产环境）

如果必须使用远程数据，考虑：
1. 将数据部署到抖音云函数
2. 或使用支持的服务器
3. 添加白名单域名

### 方案3：使用小程序本地存储

```javascript
// 首次加载后将数据存储到本地
uni.setStorageSync('productData', data)

// 后续从本地读取
const data = uni.getStorageSync('productData')
```

## 🚀 立即执行的步骤

### 步骤1：复制数据到本地

```bash
# 创建数据目录
mkdir -p /Users/tong/Code/AI/product-list-vite/src/static/data

# 复制前3页数据（测试用）
cp /Users/tong/Code/AI/ppmenu_assets_test/db/data-page1.json /Users/tong/Code/AI/product-list-vite/src/static/data/
cp /Users/tong/Code/AI/ppmenu_assets_test/db/data-page2.json /Users/tong/Code/AI/product-list-vite/src/static/data/
cp /Users/tong/Code/AI/ppmenu_assets_test/db/data-page3.json /Users/tong/Code/AI/product-list-vite/src/static/data/
```

### 步骤2：修改配置使用本地数据

需要我帮你修改代码以使用本地数据吗？

---

## 📊 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 本地静态数据 | 100% 可用，快速 | 需要更新代码 | ⭐⭐⭐⭐⭐ |
| 抖音云函数 | 灵活，可更新 | 需要部署 | ⭐⭐⭐ |
| 小程序存储 | 简单 | 容量限制 | ⭐⭐ |
| 网络请求 | 理想方案 | 抖音限制多 | ⭐ |

---

**建议：使用本地静态数据方案，这是最快最稳定的解决方案！**

需要我帮你实现吗？
